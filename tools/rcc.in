#!/bin/bash
export R_HOME=@R_HOME@
BINDIR=`dirname $0`
export LD_LIBRARY_PATH=@R_LIB_PATH@:@RCC_LIB_PATH@:${LD_LIBRARY_PATH}
export CMD="$BINDIR/rcc.bin"
if test ! "${DEBUG}xxx" == "xxx"; then
	gdb ${CMD};
else
	exec $BINDIR/rcc.bin $*;
fi;
