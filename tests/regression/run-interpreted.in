#!/bin/csh 
# @configure_input@
setenv PATH @RCC_BIN_PATH@:${PATH}
setenv RCC_R_INCLUDE_PATH @RCC_R_INCLUDE_PATH@
foreach f ( $* ) 
    echo --- $f ---                                    
	R --slave < $f | grep -v '^>' | grep -v '^+' | grep -v '^$'
end
